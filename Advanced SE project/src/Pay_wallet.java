
public class Pay_wallet implements Payment{
	public boolean pay(String name,double amount,String sp) {
		String s_wallet=name;
		//userlist user;
		//user =get_userbyname(s.wallet);
		double k_wallet=amount;
		String ss_wallet=sp;
		//if(k_wallet>=user.get_wallet())
		//	System.out.println("Congrats! "+s_wallet+" ,you have paid for the service: "+ss_wallet+"that costed: "+k_wallet+" via wallet");
		return true;
	}

}
